/**
 * MR App – app for Medical Representatives (Agents).
 * In the admin panel, these users are managed under "Agent Management".
 * Agent = MR (Medical Representative); same entity.
 */
export const APP_NAME = 'MR App';
export const ENTITY_LABEL = 'MR';
export const ENTITY_FULL = 'Medical Representative (Agent)';
export const ADMIN_AGENT_CONTEXT =
  'Your account is managed as an Agent in the admin panel.';
